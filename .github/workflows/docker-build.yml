name: Build & Push Docker Image

on:
  push:
    branches: [main]
    paths:
      # Rebuild only when these files change
      - 'Dockerfile'
      - 'requirements.txt'
      - 'frontend/**'
      - 'backend/**'
      - 'docker/**'
      - '.github/workflows/docker-build.yml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/model-designer

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # ── GitHub Actions cache (fast, stored in Actions cache storage) ────
          # Each build stage is cached separately:
          #   frontend-builder — npm ci + npm build
          #   python-builder   — pip install (heaviest, cached unless requirements.txt changes)
          #   final            — source copy only (always fast)
          cache-from: type=gha
          cache-to:   type=gha,mode=max
